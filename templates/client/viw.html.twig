{% extends 'index.html.twig' %}

{% block title %}Hello CoursierController!{% endblock %}



{% block body %}

	<link href='https://fonts.googleapis.com/css?family=Roboto:400,100,300,700' rel='stylesheet' type='text/css'>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

	<link rel="stylesheet" href={{ asset('assetss/css/style.css')}}>
 <section id="hero" class="hero d-flex align-items-center">
    
  
  </section><!-- End Hero Section --> 
  
<style>
#hero {

  height: 100px; /* Adjust the height as needed */
}
:root {
  --color-default: #0a0d13;
  --color-primary: #0d42ff;
  --color-secondary: #0e1d34;
  --darkorange: #dd7d25;
  --platinum: #e5e5e5;
  --black: #2b2d42;
  --white: #fff;
  --thumb: #edf2f4;
}

* {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}


.container {
  max-width: 1400px;
  padding: 0 15px;
  margin: 0 auto;
}

h2 {
  font-size: 32px;
  margin-bottom: 1em;
}

.cards {
  display: grid;
  grid-auto-columns: 100%;
  grid-column-gap: 10px;
  grid-auto-flow: column;
  padding: 25px 0px;
  list-style: none;
  overflow-x: scroll;
  scroll-snap-type: x mandatory;
}

.card {
  display: flex;
  flex-direction: column;
  padding: 20px;
  background: var(--white);
  border-radius: 12px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 15%);
  scroll-snap-align: start;
  transition: all 0.2s;
}

.card:hover {
  color: var(--white);
  background: var(--color-default);
}

.card .card-title {
  font-size: 20px;
}

.card .card-content {
  margin: 20px 0;
  max-width: 85%;
}

.card .card-link-wrapper {
  margin-top: auto;
}

.card .card-link {
  display: inline-block;
  text-decoration: none;
  color: white;
  background: var(--color-default);
  padding: 6px 12px;
  border-radius: 8px;
  transition: background 0.2s;
}

.card:hover .card-link {
  background: var(--color-primary);
}

.cards::-webkit-scrollbar {
  height: 12px;
}

.cards::-webkit-scrollbar-thumb,
.cards::-webkit-scrollbar-track {
  border-radius: 92px;
}

.cards::-webkit-scrollbar-thumb {
  background: var(--color-primary);
}

.cards::-webkit-scrollbar-track {
  background: var(--thumb);
}

@media (min-width: 500px) {
  .cards {
    grid-auto-columns: calc(50% - 10px);
    grid-column-gap: 20px;
  }
}

@media (min-width: 700px) {
  .cards {
    grid-auto-columns: calc(calc(100% / 3) - 20px);
    grid-column-gap: 30px;
  }
}

@media (min-width: 1100px) {
  .cards {
    grid-auto-columns: calc(25% - 30px);
    grid-column-gap: 40px;
  }
}


/* FOOTER STYLES
–––––––––––––––––––––––––––––––––––––––––––––––––– */
.page-footer {
  position: fixed;
  right: 0;
  bottom: 50px;
  display: flex;
  align-items: center;
  padding: 5px;
  z-index: 1;
}

.page-footer a {
  display: flex;
  margin-left: 4px;
}
.hor {
  display: flex; /* This sets the display mode to flex */
}

.hor > div {
  /* Optional styling for the items */
  margin: 5px;
  padding: 10px;
  border: 1px solid #ccc;
}
.card-link-wrapper a.card-link:hover {
    background: white;
    color: black;
  }
</style>
<style>
    .right-to-left {
        text-align: right;
        direction: rtl;
    }
</style>
<section class="ftco-section">
		<div class="container">
			<div class="row justify-content-center">
				<div class="col-md-6 text-center mb-5">
					<h2 class="heading-section">Mes livraison en attente</h2>
				</div>
			</div>
      
			<div class="row">
				<div class="col-md-12">
					<div class="table-wrap">
						<table class="table">
						  <thead class="thead-dark">
						    <tr>
						      <th>Reference</th>
						      <th>Prix totale</th>
						      <th>Sera livré en</th>
                  <th>&nbsp;</th>
                  <th class="right-to-left" >Actions</th>
                  <th>&nbsp;</th>
                    <th>&nbsp;</th>
						    </tr>
						  </thead>
						  <tbody>
						    <tr class="alert" role="alert">
                {% for livraison in livraisons %}
						      <th scope="row">{{ livraison.getReference() }}</th>
						      <td>{{ livraison.getPrixTotaleLivraison() }}</td>
						      <td>{{ livraison.getLivraisonDate()|date('Y-m-d ') }}</td>
						      <td>
                    <a href="{{ path('editbyclient',{'id': livraison.id}) }}" class="card-link"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> modifier  livraison</a>
                  </td>
                  <td>
                    <a href="{{ path('consultclient',{'id': livraison.id}) }}" class="card-link"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> suivre  livraison</a>
                  </td>
						      <td>	
                    <a id="link{{ loop.index }}" href="{{ path('client_annulation',{'id': livraison.id}) }}" class="card-link"   style="color: red;"><i class="fa fa-ban" aria-hidden="true"></i> Annuler la livraison</a>
				        	</td>
                  <td>
             {% set today = "now"|date('Y-m-d') %}
            {% if livraison.getLivraisonDate()|date('Y-m-d ') < today %}
            <a type="button" class="card-link signaler-link" data-toggle="modal" style="color: red;" data-target="#exampleModalCenter" data-livraison-id="{{ livraison.id }}">
              <i class="fa fa-ban" aria-hidden="true"></i>
              signaler
            </a>
              <!-- Modal -->
            <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
              <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Sélectionner la raison de signalement </h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
              <div class="modal-body">
                <form id="reportForm">
                  {% for RaisonsSignalement in RaisonsSignalements %}
                    <div class="form-check">
                      <input class="form-check-input" type="radio" name="reason" value="{{ RaisonsSignalement.id }}" id="reason{{ RaisonsSignalement.id }}">
                      <label class="form-check-label" for="reason{{ RaisonsSignalement.id }}">
                        {{ RaisonsSignalement.raison }}
                      </label>
                    </div>
                  {% endfor %}
                </form>
                    </div>
                    <div class="modal-footer">
                      <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
                      <button type="button" class="btn btn-primary" id="sendReport">Envoyer le signalement</button>
                    </div>
                  </div>
                </div>
              </div>
              <script>
                $(document).ready(function() {
                  $('.signaler-link').click(function() {
                    var livraisonId = $(this).data('livraison-id');
                    $('#exampleModalCenter').data('livraison-id', livraisonId);
                  });

                  $('#sendReport').click(function() {
                    var livraisonId = $('#exampleModalCenter').data('livraison-id');
                    var selectedReasonId = document.querySelector('input[name="reason"]:checked').value;
                    var data = {
                        livraisonId: livraisonId,
                        raisonSignalId: selectedReasonId
                    };  
                    $.ajax({
                        type: 'POST',
                        url: '{{ path('liv_non_recu') }}',
                        data: data,
                        success: function(response) {
                            console.log('livraison signaler avec succes');
                            document.querySelector('input[name="reason"]:checked').checked = false;
                            location.reload();
                        },
                        error: function(xhr, status, error) {
                            console.error('Error de signalement', error);
                        }
                      });
                    });
                });
              </script>
            {% endif %}
        </td>
						    </tr>
                {% endfor %}
						  </tbody>
						</table>
					</div>
				</div>
			</div>
       
      </ul>
        {% if error == 1 %}
                          
        <div class="alert alert-danger" role="alert">
          <p>cette livraison et déja en cours {{error}} </p>
        </div>

        {% elseif error == 2 %}
    <div class="alert alert-success" role="alert">
      <p>operation terminé avec succès .</p>
      </div>

      {% endif %}
    </div> 
		</div>
  
    
    
  <style>

.text-center:hover .buy-btn {
    color: black; /* Change text color to black when the container is hovered */
}

.text-center:hover {
    background-color: #fff; /* Change background color of the container to white on hover */
}
</style>
  
  </ul>
    
</div>

   <div class="dialog-overlay" id="confirmationDialog">
  <div class="dialog-box">
    <p>confirmer la suppression de cette livraison de votre tournée</p>
    <div class="dialog-buttons">
      <button id="confirmButton">Confirm</button>
      <button id="cancelButton" onclick="cancelAction()">Cancel</button>
    </div>
  </div>
</div>
<script>
  // Function to show the confirmation dialog for a specific link
  function showConfirmationDialog(linkId) {
    var confirmationDialog = document.getElementById('confirmationDialog');
    var link = document.getElementById(linkId);

    // Show the confirmation dialog
    confirmationDialog.style.display = 'flex';

    // Store the link's href in a data attribute for later use
    confirmationDialog.setAttribute('data-href', link.href);
  }

  // Function to handle confirmation for a specific link
  function confirmAction() {
    var confirmationDialog = document.getElementById('confirmationDialog');

    // Get the stored href from the data attribute
    var href = confirmationDialog.getAttribute('data-href');

    // Navigate to the link's href if confirmed
    if (href) {
      window.location.href = href;
    }
  }

  // Attach event listeners to each annuler button
  {% for livraison in livraisons %}
    document.getElementById('link{{ loop.index }}').addEventListener('click', function(event) {
      event.preventDefault(); // Prevent the default link behavior
      showConfirmationDialog('link{{ loop.index }}');
    });
  {% endfor %}

  // Attach event listener to the confirm button
  document.getElementById('confirmButton').addEventListener('click', function(event) {
    event.preventDefault(); // Prevent the default button behavior
    confirmAction();
  });

  // Function to cancel the action
  function cancelAction() {
    var confirmationDialog = document.getElementById('confirmationDialog');
    // Hide the confirmation dialog
    confirmationDialog.style.display = 'none';
  }
</script>
	</section>
<footer class="page-footer">
  <span>made by </span>
  <a href="https://georgemartsoukos.com/" target="_blank">
    <img width="24" height="24" src="https://assets.codepen.io/162656/george-martsoukos-small-logo.svg" alt="George Martsoukos logo">
  </a>
</footer>
<style>
  /* Styles for the custom dialog */
  .dialog-overlay {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 9999;
  
  justify-content: center; /* Center horizontally */
  align-items: center; /* Center vertically */
}

.dialog-box {
  background-color: #fff;
  border-radius: 5px;
  padding: 20px;
  max-width: 400px;
  text-align: center;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
}

  .dialog-buttons {
    margin-top: 20px;
  }

  /* Styling for buttons */
  #confirmButton {
    background-color: green;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  #confirmButton:hover {
    background-color: darkgreen;
  }

  #cancelButton {
    background-color: red;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  #cancelButton:hover {
    background-color: darkred;
  }
</style>
   
   
{% endblock %}