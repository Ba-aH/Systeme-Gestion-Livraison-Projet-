<table>
                        <thead>
                            <tr>
                                <td>Courier ID</td>
                                <td>Nom</td>
                                <td>Prenom</td>
                                <td>Disponible de</td>
                                <td>Jusqu'a</td>
                                <td>Actions</td>
                            </tr>
                        </thead>

                        <tbody>
                        {% for courier in couriers %}
                        <tr>
                            <td>{{ courier.id }}</td>
                            
                            {# <td>{{ courier.nb_livraison  }}</td> #}
                            
                            
                            {# <td> <p> {{ statutLivraison.getLivraisonDate()|date('Y-m-d H:i:s') }}</p> #}

                            
                            <td>
                            {# <a href="{{ path('/') }}">Add to his tour</a> #}
                            </td>
                        </tr>
                            {% else %}
                                <tr>
                                    <td colspan="8">no records found</td>
                                </tr>
                            {% endfor %}

                        
                    </table>