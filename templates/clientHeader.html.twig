{% extends 'clientHeader.html.twig' %}

{% block body %}
   
 <!-- ======= Header ======= -->
  <header id="header" class="header d-flex align-items-center fixed-top">
    <div class="container-fluid container-xl d-flex align-items-center justify-content-between">

      <a href="index.html" class="logo d-flex align-items-center">
        <!-- Uncomment the line below if you also wish to use an image logo -->
        <!-- <img src="assets/img/logo.png" alt=""> -->
        <h1>Logis</h1>
      </a>

      <i class="mobile-nav-toggle mobile-nav-show bi bi-list"></i>
      <i class="mobile-nav-toggle mobile-nav-hide d-none bi bi-x"></i>
      <nav id="navbar" class="navbar">
        <ul>
          {% if user is null %}
            <li><a href="{{path('appClient_index')}}" class="active">Home</a></li>
            <li><a href="#" onclick="scrollToDiv('about')">About</a></li>
            <li><a href="#" onclick="scrollToDiv('service')" >Services</a></li>
            <li><a href="#"onclick="scrollToDiv('horizontal-pricing')">Pricing</a></li>
            <li><a href="{{ path('contact') }}">Contact</a></li>
            <li><a class="get-a-quote" href="{{ path('app_login') }}">Login</a></li>
          {% endif %}
          {% if user is not null %}
        </li>
            <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow notifications">
                <li class="notification-item">
                <div id="notification-list"></li>
            </ul>
            <li><a href="{{path('afficher_livraisons')}}">Home</a></li>
            <li><a href="{{path('afficher_livraisons')}}">Mes Livraisons</a></li>
            <ul>
              <li class="dropdown"><a href="#"><span>Profile</span> <i class="bi bi-chevron-down dropdown-indicator"></i></a>
                <ul>
                  <li><a href="{{path('clientProfile')}}">Mon Profile</a></li>
                  <li><a href="{{path('app_logout')}}">Logout</a></li>
                </ul>
              </li>
            </ul>
          {% endif %}
        </ul>
      </nav><!-- .navbar -->

    </div>
  
  </header>
  {% endblock %}