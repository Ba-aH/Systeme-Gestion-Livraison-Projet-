{# templates/login/index.html.twig #}
{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
     <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <!-- Leaflet JavaScript -->
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css?family=Karla:400,700&display=swap');

        .font-family-karla {
            font-family: karla;
        }
        .error-message {
            color: #ff0000; /* Set your desired color for the error message */
            font-size: 14px; /* Set your desired font size */
            margin-top: 8px; /* Add some spacing above the error message */
        }
    </style>
    

    <body class="bg-white font-family-karla h-screen">

        <div class="w-full flex flex-wrap">

            <!-- Login Section -->
            <div class="w-full md:w-1/2 flex flex-col">

                
            <div class="flex flex-col justify-center md:justify-start my-auto pt-8 md:pt-0 px-8 md:px-24 lg:px-32">
                <p class="text-center text-3xl">Welcome.</p>

        <form action="{{ path('app_login') }}" method="post">
            <label for="username" class="text-lg">Email:</label>
            <input type="email" id="username"placeholder="your@email.com" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 mt-1 leading-tight focus:outline-none focus:shadow-outline"  name="_username" value="{{ last_username }}">

            <label for="password" class="text-lg">Password:</label>
            <input type="password" id="password" name="_password" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 mt-1 leading-tight focus:outline-none focus:shadow-outline">
           
            <a href="#" onclick="sendResetLink(event)" class="text-blue-500 hover:text-blue-700 text-sm">Forgot Password</a><br>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            {% if error %}
                <div class="error-message text-red-500">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            <button type="submit" class="bg-blue-500 text-white font-bold text-lg hover:bg-blue-700 p-2 mt-8 rounded-md">login</button>
        </form>

        <div class="text-center pt-12 pb-12">
                    <p>Don't have an account? <a href="{{path('select_register')}}" class="underline font-semibold">Register here.</a></p>
                </div>
            </div>

        </div>

        <!-- Image Section -->
        <div class="w-1/1 ">
            <img class="object-cover w-full h-screen hidden md:block" src="{{ asset('images/livreur.jpg') }}" alt="Background" />
        </div>
    </div>
{# <button id="start-btn">STOP Fetching</button>
<button id="startt">START Fetching</button> #}
    <script>
        function submitForm() {
            var formData = {
                username: document.getElementById('username').value,
                password: document.getElementById('password').value,
            };

            fetch('/api/login_check', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(formData),
            })
            .then(response => response.json())
            .then(data => {
                // Handle the token response
                console.log('Token:', data.token);
                localStorage.setItem('token', data.token);

                // Redirect to another page if needed

                // Send the token to the server
                fetch('/api/login', {
                    method: 'GET',
                    headers: {
                        'Authorization': 'Bearer ' + data.token,
                    },
                })
                .then(response => response.json())
                .then(data => {
                    // Handle the response from the server
                    console.log('Server Response:', data);
                })
                .catch((error) => {
                    console.error('Error:', error);
                });
            })
            .catch((error) => {
                console.error('Error:', error);
            });

            // Function to get user data can be outside the submitForm function
            function getUserData() {
                var token = localStorage.getItem('token');

                if (token) {
                    var userData = JSON.parse(atob(token.split('.')[1]));

                    console.log('User Data:', userData);

                    // Use the user data as needed
                } else {
                    console.error('Token not found.');
                }
            }
            getUserData();
        }

        function sendResetLink(event) {
            event.preventDefault(); 
            var email = document.getElementById('username').value;
            var resetLink = "{{ path('resetPass') }}?email=" + email;
            window.location.href = resetLink;
        }
    </script>
 {# <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <style>
        #map {
            height: 400px;
        }
    </style>
   <div id="map"></div>

<script>

            var latitude =  36.883258 ; // Replace with your latitude
            var longitude = 10.203859; // Replace with your longitude
      
            // Create a map
            var map = L.map('map').setView([latitude, longitude], 13);

            // Add a tile layer (you can choose your preferred tile provider)
            L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
            }).addTo(map);

            // Add a marker to the map
            var marker = L.marker([latitude, longitude]).addTo(map);
         
            // Function to convert coordinates to address using Nominatim API
            function getReverseGeocode(lat, lng) {
                fetch(`https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lng}`)
                    .then(response => response.json())
                    .then(data => {
                        // Log the address to the console
                        console.log('Converted Address:', data.display_name);
                    })
                    .catch(error => console.error('Error:', error));
            }

            // Call the function with your latitude and longitude values
            getReverseGeocode(latitude, longitude);
   
var intervalId;

function startFetchingData() {
    // Fetch data initially when the page loads
    fetchData();

    // Fetch new data every 30 seconds
    intervalId = setInterval(fetchData, 3000); // 30 seconds in milliseconds
}

function stopFetchingData() {
    clearInterval(intervalId);
}

function fetchData() {
    $.ajax({
        url: '{{ path('send') }}',
        type: 'GET',
        success: function(response) {
            // This function executes when the AJAX request is successful
            console.log('Response received:', response);
            const latitude = response.latitude;
            const longitude = response.longitude;
        marker.setLatLng([latitude, longitude]);
        getReverseGeocode(latitude, longitude);



        },
        error: function(xhr, status, error) {
            // This function executes if there's an error with the AJAX request
            console.error('Error fetching data:', error);
        }
    });
}

// Call startFetchingData when you want to start fetching data


 if ($('#start-btn').length === 0) {
            console.error('Button not found.');
        }

        // Add onclick event to the button to stop fetching data
        $('#start-btn').on('click', function() {
            stopFetchingData();
            console.log('Fetching stopped.');
        }); 



        if ($('#startt').length === 0) {
            console.error('Button not found.');
        }

        // Add onclick event to the button to stop fetching data
        $('#startt').on('click', function() {
          startFetchingData();
            console.log('Fetching started.');
        });  





</script> #}
    

{% endblock %}